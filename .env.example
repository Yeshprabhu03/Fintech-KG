# Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# GitHub (for auto-commit via Actions — set in repo secrets, not here)
# GITHUB_TOKEN is auto-provided by Actions

# Optional: Umami analytics (free, self-hosted)
UMAMI_WEBSITE_ID=your_umami_id_here
